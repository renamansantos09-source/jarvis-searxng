use_default_settings: true

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "jarvis-searxng-secret-key-change-later"
  limiter: false
  image_proxy: true

search:
  formats:
    - html
    - json

ui:
  static_use_hash: true

outgoing:
  request_timeout: 5.0
